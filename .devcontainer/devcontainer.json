{
    "name": "rust-taglib-picture",
    "image": "rust:1.56.0-slim-buster",
    "postStartCommand": "apt update && apt install -y git ssh",
    "runArgs": [
        "-v", "${env:XDG_RUNTIME_DIR}/gnupg/S.gpg-agent:/root/.gnupg/S.gpg-agent",
    ],
    "extensions": [
        "rust-lang.rust",
        "vadimcn.vscode-lldb"
    ],
} 